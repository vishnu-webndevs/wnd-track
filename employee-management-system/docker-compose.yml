version: "3.8"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8081:8081"
    volumes:
      - tracker_2026_storage:/var/www/html/storage

volumes:
  tracker_2026_storage:
