version: "3.8"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - tracker_storage:/var/www/html/storage

volumes:
  tracker_storage:
